---
import '../assets/styles/global.css'
import '../assets/scss/index.scss'
import SiteMeta from '~/components/core/SiteMeta.astro'
import { ClientRouter } from 'astro:transitions'
import Header from '~/components/core/Header.astro'
import Footer from '~/components/core/Footer.astro'
import { withBasePath } from '~/lib/url'

const {
  title = 'Guia acessibilidade web',
  description = 'Guia prático para implementar, validar e revisar acessibilidade na web com exemplos e critérios reais.',
  url = Astro.url,
  image = '/guia-acessibilidade-web.png',
  author = 'Gabriel Teixeira da Silva',
} = Astro.props
---

<html lang="pt-BR" dir="ltr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <link rel="icon" type="image/svg+xml" href={withBasePath('/favicon.svg')} />
    <SiteMeta title={title} description={description} url={url} image={image} author={author} />
    <ClientRouter />
  </head>
  <body>
    <Header />
    <main id="main-content" transition:animate="fade">
      <slot />
    </main>
    <Footer />
  </body>
</html>
